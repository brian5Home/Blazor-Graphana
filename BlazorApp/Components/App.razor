<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <style>
        /* Critical layout colors – always applied so pages have color even if app.css fails to load */
        body { margin: 0; font-family: 'Segoe UI', system-ui, sans-serif; color: #0f172a; background: #f1f5f9 !important; }
        .layout { display: flex; min-height: 100vh; }
        .sidebar { width: 240px; min-width: 240px; background: #0f172a !important; color: #f8fafc !important; padding: 1.25rem 0; display: flex; flex-direction: column; gap: 0.25rem; }
        .brand { font-size: 1rem; font-weight: 700; padding: 0 1.25rem 1rem; margin-bottom: 0.5rem; border-bottom: 1px solid rgba(255,255,255,0.15); }
        .nav-link { display: block; padding: 0.6rem 1.25rem; color: rgba(248,250,252,0.9); text-decoration: none; }
        .nav-link:hover { background: rgba(255,255,255,0.1); color: #fff; }
        .nav-link.active { background: #2563eb !important; color: #fff !important; font-weight: 500; }
        .main { flex: 1; padding: 1.5rem 2rem; max-width: 1200px; background: #f1f5f9 !important; color: #0f172a !important; }
    </style>
    <link rel="stylesheet" href="/app.css" />
    <link rel="stylesheet" href="/BlazorApp.styles.css" />
    <HeadOutlet />
</head>

<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
</body>

</html>
